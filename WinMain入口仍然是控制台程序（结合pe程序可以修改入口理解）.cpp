#include <iostream>
#include <string.h>
#include <windows.h>

using namespace std;

int WinMain(HINSTANCE hInstance, HINSTANCE hPreInstance, LPSTR lpCmdLine, int nCmdShow){
	cout << "xx" << endl;  //即使注释掉这一句，还是会有个窗口一闪而过
	return 0;
}